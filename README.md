mmdvm_connect 
--------------------------------------
connect MMDVM hotspot (or repeater) directly to another MMDVM hotspot (or repeater).
The MMDVM hotspot running **mmdvm_connect** app serves as a virtual master instance. Other hotspots connect to it as slaves using its IP address and port 62931.


(DMR mode only)


